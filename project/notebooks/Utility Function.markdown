## Reusable Utility Function

The `parse_date_column()` function converts date columns into pandas
datetime format. This function will be useful in later stages when
historical SPY market data are imported and prepared for time-series
analysis.